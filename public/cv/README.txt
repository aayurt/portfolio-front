Resume / CV

PREFERRED: upload the CV in the Payload admin (backend) instead of this folder.

1. Payload admin → Media → Create new → upload your PDF
2. Payload admin → Tenants → your tenant → CV field → select the uploaded PDF
3. The Resume buttons (header, homepage hero, About page) then link to the
   Payload media file route, e.g.
   https://aayurtshrestha.com.np/admin/api/media/file/<filename>

Fallback: you may also drop a PDF here named Aayurt-Shrestha-CV.pdf and the
buttons link to /cv/Aayurt-Shrestha-CV.pdf when no tenant CV is set.
